<html><head></head><body><h1>Goota</h1><p>検索したいタグ名を入れると該当するQiitaの記事を検索します。いいね(stocks)が100以上のものを検索結果として出力します。</p><div class="search-form"><form action="/goota/search" method="GET">    <label for="forQueryForm">Query</label>    <input type="text" name="query" value="" placeholder="ex. go,golang">    <input type="submit" value="検索する" class="search-btn"></form></div><div class="search-result"></div><script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script></body></html><script>$(".search-form form input.search-btn").on("click", function (evt) {  evt.preventDefault()  $(".search-result").empty()  var url = $(".search-form form").attr("action")  var d = $(".search-form form").serialize();  $.ajax({    url: url,    type: "get",    timeout: 5000,    data: d  }).done(function (data) {    items = JSON.parse(data);    Object.keys(items).forEach(function (idx) {      item = items[idx];      var resHtml = $(".search-result")      resHtml.append("<p>")      resHtml.append("<b>Title: " + item.title + "</b></br>")      resHtml.append("AutherName: " + item.user.name +"</br>")      resHtml.append("URL -> <a href='"+ item.url + "' target ='_blank'>Qiitaのページはこちら</br>")      resHtml.append("</p>")    })  }).fail(function (err) {    console.log("err:"+err);  })});</script>